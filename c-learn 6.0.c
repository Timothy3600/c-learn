#include <stdio.h>

int main() {
    /*int x,a,b,sum;
    do{
        printf("Please enter the first number\n");
        scanf("%d",&a);
        printf("Please enter a second number|n");
        scanf("%d",&b);
        sum=a+b;
        printf("The sum of %d and %d is %d\n",a,b,sum);
        printf("would you like to continue??\n press to continue or  to exit");
        scanf("%d",&x);
          }
    while(x==1);
*/
int i, x, y, sum;
 i=1;
 do{
        sum=x+y;
 printf("The sum is %d\n",sum);
 i++;
 }
 while(i<=5);

return 0;
}
